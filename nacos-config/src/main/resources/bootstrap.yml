server:
  port: 18003
spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置 Nacos 地址
      config:
        server-addr: localhost:8848 #配置 Nacos 地址
        file-extension: yaml  #指定配置文件的格式 yaml
        group: DEV_GROUP    # 指定组名 注意:区分大小写
        namespace: ee31597c-cbca-47d6-a50d-f52f0b7dec02 # 指定命名空间
